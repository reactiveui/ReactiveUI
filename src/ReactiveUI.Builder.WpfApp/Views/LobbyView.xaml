<UserControl x:Class="ReactiveUI.Builder.WpfApp.Views.LobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:rxui="clr-namespace:ReactiveUI;assembly=ReactiveUI"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Display Name:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="240" x:Name="DisplayNameBox" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <TextBox Width="240" x:Name="RoomNameBox" />
            <Button Content="Create Room" Margin="8,0,0,0" x:Name="CreateRoomButton" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,8">
            <Button Content="Delete Selected Room" x:Name="DeleteRoomButton" />
        </StackPanel>
        <ListBox Grid.Row="3" x:Name="RoomsList" DisplayMemberPath="Name" />
    </Grid>
</UserControl>
