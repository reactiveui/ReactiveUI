<Project>
  <!-- Replaces the WindowsAppSDK PRI generation target with a no-op so MakePri.exe is never invoked off-Windows.
       The managed assembly still compiles, enabling reference/CI validation on Linux and macOS. -->
  <Target Name="_GenerateProjectPriFile" />
</Project>
